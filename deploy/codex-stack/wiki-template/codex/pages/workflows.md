# Workflows

## Локальная задача

Codex Desktop читает проект, вносит ограниченные изменения и запускает проверки.

## Удалённая задача

Codex Desktop создаёт или продолжает поток на Kate, где выполняются Linux-сборка,
тесты и команды Codex CLI. Результат и Git diff проверяются до публикации.
